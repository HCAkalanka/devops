# Copy to .env (or .env.local) and adjust per environment
# Local (via docker-compose + Vite proxy):
VITE_API_URL=http://98.92.228.95:5000

# Alternatively, point directly at a backend:
# VITE_API_URL=http://localhost:5000/api
